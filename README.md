# GERMiFY

**GERMiFY** — мини-социальная сеть на **Django** (MVP): лента постов, лайки, комментарии и ответы, личные сообщения, профили и подписки, сообщества.  
Фронтенд: **Django templates + ванильный JS + CSS** (без React/Vue).  
Проект находится в стадии MVP: функционал уже рабочий, дальше планируется чистка директории и нормализация логики/данных.

---

## 🚀 Возможности

### 📰 Лента (Feed)
- Лента постов (пагинация/подгрузка)
- Создание/удаление/редактирование постов
- “Умный” поиск по ленте: сортировка по похожести, поиск по тексту и по автору

### 📎 Вложения
- Вложения к постам: файлы/картинки/видео/аудио
- Хранение в `media/`
- Превью и корректный вывод медиа

### ❤️ Лайки
- Лайки на посты
- Лайки на комментарии

### 💬 Комментарии
- Комментарии к постам
- Ответы на комментарии (replies)

### ✉️ Личные сообщения
- Inbox со списком чатов
- Чат внутри диалога
- Отправка сообщений без перезагрузки страницы (XHR)
- Вложения в сообщениях (как в постах)
- Индикатор “прочитано / не прочитано” (read receipts)

### 👤 Профили
- Аватар
- Bio/описание
- Статистика/подписчики/подписки

### ➕ Подписки
- Follow / Unfollow
- Отображение связи между пользователями

### 👥 Сообщества
- Создание сообщества
- Вступление/выход
- Участники и админы
- Посты внутри сообщества

---

## 🧱 Технологии

- **Python 3.x**
- **Django 5.x**
- **MySQL 8**
- **Nginx** (в Docker)
- **Daphne / ASGI**
- Шаблоны: Django Templates
- JS: ванильный JavaScript
- CSS: кастом + Bootstrap-подобный стиль

---

## 📁 Структура проекта

```text
germify/
  docker-compose.yml
  .env
  nginx/
    conf.d/
      germify.conf

  backend/
    Dockerfile
    entrypoint.sh
    manage.py
    requirements.txt
    germify/          # настройки проекта, urls, asgi
    core/             # основное приложение: models, views, templates, static
    media/            # пользовательские файлы (в docker монтируется в volume)
    staticfiles/      # результат collectstatic (в docker монтируется в volume)
```

Ключевые части:
- `backend/core/` — логика приложения (лента, профили, сообщения, сообщества)
- `backend/core/templates/` — HTML-шаблоны
- `backend/core/static/` — JS/CSS
- `backend/germify/settings.py` — настройки Django
- `nginx/conf.d/germify.conf` — nginx проксирует запросы к Django и отдаёт `/static/`, `/media/`

---

## ⚙️ Как работает проект (в общих чертах)

### Статика и медиа
- Django собирает статику командой `collectstatic` в `backend/staticfiles/`
- Nginx отдаёт:
  - `/static/` → из `staticfiles`
  - `/media/` → из `media`

### Django + Nginx
- Nginx принимает запросы на `http://localhost`
- Проксирует всё (кроме `/static/` и `/media/`) на Django (контейнер `web`)
- Django работает под **ASGI** через Daphne

### База данных
- Используется **MySQL**
- В Docker поднимается отдельным контейнером `db`
- Django подключается к БД по hostname `db` (внутри docker-сети)

---

## 🐳 Запуск через Docker (рекомендуется)

> `.env` уже есть в проекте — создавать/копировать не нужно.

### 1) Установить зависимости
Установи:
- Docker Desktop
- Git (если клонируешь репозиторий)

Проверка:
```bash
docker --version
docker compose version
```

### 2) Запуск
В корне проекта (где `docker-compose.yml`):
```bash
docker compose up --build
```

После запуска сайт доступен:
- http://localhost/

### 3) Создать админа
В другом терминале:
```bash
docker compose exec web python manage.py createsuperuser
```

Админка:
- http://localhost/admin/

### 4) Остановить
```bash
docker compose down
```

### 5) Полный сброс (включая базу)
Если нужно “с нуля”:
```bash
docker compose down -v
docker compose up --build
```

---

## 🧪 Тесты (pytest)

Запуск тестов внутри Docker (если pytest установлен в requirements):
```bash
docker compose exec web pytest -q
```

---

## 🛠 Полезные команды (Docker)

Статус контейнеров:
```bash
docker compose ps
```

Логи:
```bash
docker compose logs -f web
docker compose logs -f nginx
docker compose logs -f db
```

Зайти в контейнер `web`:
```bash
docker compose exec web bash
```

Миграции вручную:
```bash
docker compose exec web python manage.py makemigrations
docker compose exec web python manage.py migrate
```

Собрать статику вручную:
```bash
docker compose exec web python manage.py collectstatic --noinput
```

---

## 🧩 Локальный запуск без Docker (если нужно)

Не рекомендуется, потому что придётся ставить MySQL и зависимости руками.

Примерный план:
1) Создать venv  
2) Установить зависимости: `pip install -r backend/requirements.txt`  
3) Поднять MySQL локально и прописать доступы в `settings.py` / `.env`  
4) `python manage.py migrate`  
5) `python manage.py runserver`

---

## 🧯 Частые проблемы

### Порт 80 занят
Если `http://localhost` не открывается и Docker ругается на порт — занят порт `80` (IIS/другой сервис).

Решение: поменяй в `docker-compose.yml` у nginx:
```yaml
ports:
  - "8080:80"
```

Тогда сайт будет:
- http://localhost:8080/

### “bad interpreter” / entrypoint.sh
Если Docker жалуется на `entrypoint.sh`, значит у файла неправильные переносы строк.  
Решение: пересохранить `entrypoint.sh` в **LF** (не CRLF).

---

## 📌 Статус проекта
MVP готов и работает. Дальше планируется:
- чистка/нормализация структуры и дублей
- улучшение UX/внешнего вида
- улучшение real-time в чатах
- расширение поиска/тэгов и модерация контента
